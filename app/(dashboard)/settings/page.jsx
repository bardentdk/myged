import SettingsScreen from '@/components/screens/SettingsScreen';

export const metadata = { title: "Paramètres — Mar'my GED" };

export default function SettingsPage() {
  return <SettingsScreen />;
}
