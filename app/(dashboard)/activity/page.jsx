import ActivityScreen from '@/components/screens/ActivityScreen';

export const metadata = { title: "Activité — Mar'my GED" };

export default function ActivityPage() {
  return <ActivityScreen />;
}
