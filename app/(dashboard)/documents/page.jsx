import ExplorerScreen from '@/components/screens/ExplorerScreen';

export const metadata = { title: "Documents — Mar'my GED" };

export default function DocumentsPage() {
  return <ExplorerScreen />;
}
